cmake_minimum_required( VERSION 3.16.3 )

project( kalkulator_macierzy )
#2
#aux_source_directory(. SRC_LIST)

#add
set(wxWidgets_ROOT_DIR "C:\\Program Files (x86)\\wxWidgets\\")
SET(wxWidgets_LIB_DIR "C:\\Program Files (x86)\\wxWidgets\\lib\\")


#2
#set(wxWidgets_CONFIGURATION mswu)
#find_package(wxWidgets COMPONENTS core base REQUIRED)
#include(${wxWidgets_USE_FILE})
#add_executable(${PROJECT_NAME} ${SRC_LIST})
#target_link_libraries(${PROJECT_NAME} ${wxWidgets_LIBRARIES})

#1
find_package(wxWidgets REQUIRED gl core base OPTIONAL_COMPONENTS net) 
#core base ==std
include(${wxWidgets_USE_FILE})
# and for each of your dependent executable/library targets:
#target_link_libraries(app ${wxWidgets_LIBRARIES})
target_link_libraries(app PRIVATE ${wxWidgets_LIBRARY})


#include
include_directories( ./include/matrix ./src )

#target
add_executable( app.out ./src/app.cpp ./src/matrix.cpp ./src/matrix_exceptions.cpp )

#libs







